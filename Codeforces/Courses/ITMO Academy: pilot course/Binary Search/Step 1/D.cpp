#include <bits/stdc++.h>

using namespace std;

void setIO() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
}


int main() {
    setIO();
    int n;
    cin >> n;
    multiset<int> a;
    int num;
    for (int i = 0; i < n; ++i) {
        cin >> num;
        a.insert(num);
    }
    int k, l, r;
    cin >> k;
    vector<int> sol(k);
    for (int i = 0; i < k; ++i) {

    }
    return 0;
}
