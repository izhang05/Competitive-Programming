#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
#ifdef DEBUG
    //freopen("solution.out", "w", stdout);
    freopen("1.in", "r", stdin);
#endif

    string s;
    cin >> s;
    int q;
    cin >> q;
    int l[q], r[q];
    for (int i = 0; i < q; ++i) {
        cin >> l[i] >> r[i];

    }
    return 0;
}
