#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    long long x;
    cin >> t;
    while (t--) {
        cin >> x;
        cout << 1 << " " << x - 1 << "\n";
    }
    return 0;
}