/*
ID: izhang01
TASK: mirrors
LANG: C++11
*/

#include <bits/stdc++.h>

using namespace std;

void setIO() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("mirrors.in", "r", stdin);
    freopen("mirrors.out", "w", stdout);
}


int main() {
    setIO();
    int n, a, b;
    cin >> n >> a >>b;

    return 0;
}
