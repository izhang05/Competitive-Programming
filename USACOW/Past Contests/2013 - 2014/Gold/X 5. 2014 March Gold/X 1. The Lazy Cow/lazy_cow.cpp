/*
ID: izhang01
TASK: lazy
LANG: C++11
*/

#include <bits/stdc++.h>

using namespace std;

void setIO() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cin.exceptions(istream::failbit);
    freopen("lazy.in", "r", stdin);
    freopen("lazy.out", "w", stdout);
    freopen("lazy.out", "w", stderr);
}
//#define DEBUG

int main() {
    setIO();


    return 0;
}
