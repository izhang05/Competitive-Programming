#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
//    freopen("solution.out", "w", stdout);
//    freopen("1.in", "r", stdin);
    int n;
    while (cin >> n) {
        if (n == 42) {
            break;
        } else {
            cout << n << "\n";
        }
    }
    return 0;
}
