#include <iostream>

using namespace std;

int main() {
    int t, n, m;
    cin >> t;
    while (t--) {
        cin >> n >> m;
        int sequence[n];
        for (int i = 0; i < n; ++i) {
            cin >> sequence[i];

        }

    }
    return 0;
}