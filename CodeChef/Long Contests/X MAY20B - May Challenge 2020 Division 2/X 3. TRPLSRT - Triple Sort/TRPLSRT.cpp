#include <iostream>

using namespace std;

int main() {
//    freopen("solution.out", "w", stdout);
    freopen("1.in", "r", stdin);
    int t, n, k;
    cin >> t;
    while (t--) {
        cin >> n >> k;
    }
    return 0;
}
